export const metadata = {
  title: 'ODZ Platform',
  description: 'Restoran zinciri saha denetim platformu',
};

export default function TenantLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
